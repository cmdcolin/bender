---
// Google Analytics on every page, once the visitor has said yes. The id, the
// notice that asks and the loader are in src/analytics.ts.
---

<script>
  import { startAnalytics } from '../analytics'

  // CROSS_REPO_SYNC(analytics-snippet)
  startAnalytics()
  // CROSS_REPO_SYNC_END(analytics-snippet)
</script>
